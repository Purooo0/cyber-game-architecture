/**
 * Tileset Configuration
 * Semua tilesets sekarang menggunakan dimensi standar yang kelipatan 64x64
 */

// Placeholder untuk compatibility - tidak ada non-standard tilesets lagi
export function isNonStandardTileset(tilesetName: string): boolean {
  return false
}
